package com.syntax.JavaBasics.class21;

public class CalculateArea {
    /*
    Create 1 class in which create a methods that will
     calculate the area (volume in case of box) of
Rectangle
Square
Box
     */
    static void AreaRectangle(double length, double width) {
        System.out.println(length*width);
    }
public void calculateVolume(double length, double width, double height) {
System.out.println(length*width*height);
}
}